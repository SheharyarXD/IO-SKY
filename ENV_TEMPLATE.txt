# =============================================================================
# IO SKY — Environment Template
# -----------------------------------------------------------------------------
# Rename this file to `.env.example` (then copy to `.env`) inside your own repo.
# Fill in real values in `.env`. NEVER commit a real `.env`.
# Variable NAMES only — no secrets are included in this template.
#
# The frontend can ONLY read variables prefixed with VITE_. Never place a
# secret behind a VITE_ prefix unless it is intentionally public
# (e.g. logo URL, public app id).
#
# ENVIRONMENT SEPARATION (Milestone 1, RM-61..63): keep one env file per
# environment — e.g. `.env.development`, `.env.staging`, `.env.production`
# (or your host's equivalent secret-store separation) — and NEVER copy a
# production DATABASE_URL/SUPABASE_SECRET_KEY/JWT_SECRET into a development
# or staging file. Every one of the *.env* filename variants is already
# gitignored (see .gitignore) so none of them are committable by accident.
# Whether staging/production share one Supabase project (separated by RLS
# and application-level scoping only) or use two fully separate Supabase
# projects is a decision for whoever owns the Supabase billing/account — not
# something this template can decide; document the choice here once made.
# =============================================================================

# -----------------------------------------------------------------------------
# CORE (required)
# -----------------------------------------------------------------------------
# Supabase PostgreSQL direct connection string (Milestone 1, RM-43: migrated
# off MySQL/TiDB). Get this from the Supabase dashboard: Project Settings ->
# Database -> Connection string. This is a SEPARATE credential from the
# SUPABASE_* API keys below — the API keys authenticate against the Auth/
# REST/Storage APIs, this is the actual Postgres password, required for
# drizzle-kit migrate/push and the app's own postgres-js connection.
# Format: postgresql://postgres:[password]@[host]:5432/postgres
DATABASE_URL=

# 32+ char random secret. Signs session cookies, impersonation tokens,
# HMAC booking-action tokens, and derives the MFA at-rest encryption key.
# Generate with: openssl rand -base64 48
# WARNING: rotating this invalidates ALL sessions + pending booking links.
JWT_SECRET=

# Node environment: "development" | "production"
NODE_ENV=production

# Optional explicit port. The server auto-selects 3000-3019 if unset.
PORT=

# Public base URL of the site (used to build email links: cancel/reschedule).
PUBLIC_BASE_URL=https://iosky.nl
VITE_PUBLIC_BASE_URL=https://iosky.nl

# -----------------------------------------------------------------------------
# SUPABASE (Milestone 1 migration target — RM-41/42)
# Database: DATABASE_URL above must be the project's direct Postgres
# connection string (Project Settings → Database) once the schema/driver
# migration (RM-43/46) lands — the API keys below do NOT substitute for it,
# they authenticate the Auth/REST/Storage APIs, not a raw Postgres connection.
# -----------------------------------------------------------------------------
SUPABASE_URL=
# New-format Supabase API keys (publishable/secret), not the legacy
# anon/service_role JWT pair.
SUPABASE_PUBLISHABLE_KEY=
# Server-only. Full data-plane access (bypasses RLS) — never expose to the
# client, never prefix with VITE_.
SUPABASE_SECRET_KEY=
# Used for local/manual verification of Supabase-issued JWTs if ever needed
# outside the supabase-js SDK's own session handling.
SUPABASE_JWKS_URL=

# Milestone 2 §2.3 — BLOCKED, external dashboard configuration, no env var
# here and no code path in this repo: Supabase Auth's own password-reset
# email (client/src/pages/Login.tsx already calls
# supabase.auth.resetPasswordForEmail() — RM-50) is sent BY Supabase's
# servers, not by this app's backend, using Supabase's default mailer
# (a few emails/hour, not production-suitable, generic unbranded template)
# unless Custom SMTP is configured. To route it through the same Resend
# infrastructure as every other email in this app: Supabase Dashboard →
# Authentication → Emails → SMTP Settings → enable Custom SMTP, host
# smtp.resend.com, port 587, username "resend", password = the Resend API
# key. Also consider replacing Supabase's default email template text
# under Authentication → Emails → Templates to match this app's branding
# (server/email.ts's HTML templates are the reference for tone/styling —
# Supabase's template editor is separate from this codebase and can't be
# driven from here). Needs live Supabase dashboard access this environment
# doesn't have; nothing to implement in code once the account owner does this.

# Frontend — Vite only exposes VITE_-prefixed vars to the browser bundle.
# The publishable key is safe to expose client-side by design.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# -----------------------------------------------------------------------------
# LLM (Milestone 2 §2.2 — direct provider integration, replacing the
# Forge/Manus LLM proxy). Powers the AI Scan scoring engine
# (server/_core/aiScanScoring.ts). Provider-neutral: server/_core/llm.ts
# speaks OpenAI's Chat Completions wire format (messages[], response_format
# json_schema) directly — no separate gateway/proxy in front of it anymore.
# Any provider that accepts that format at a URL works with no code change:
# OpenAI itself, Azure OpenAI, Google's Gemini OpenAI-compatibility
# endpoint, OpenRouter, Together, Groq, etc.
#
# BLOCKED — production activation: no provider has been selected yet. Pick
# one, fill in its full chat-completions endpoint + API key + model name
# below. See MILESTONE2_PROGRESS.md §2.2 for the exact decision needed.
# -----------------------------------------------------------------------------
# Full endpoint URL, e.g. https://api.openai.com/v1/chat/completions
LLM_API_URL=
LLM_API_KEY=
# Model name as the chosen provider expects it, e.g. "gpt-4o-mini",
# "gpt-4o", or a Gemini/OpenRouter model id. Defaults to "gpt-4o-mini" if unset.
LLM_MODEL=
# Request timeout in ms. Defaults to 60000 (60s) if unset.
LLM_TIMEOUT_MS=

# -----------------------------------------------------------------------------
# AUTHENTICATION
# Local email+password login works with only DATABASE_URL + JWT_SECRET.
# The Manus OAuth portal below is OPTIONAL convenience SSO.
# -----------------------------------------------------------------------------
OAUTH_SERVER_URL=
VITE_OAUTH_PORTAL_URL=
VITE_APP_ID=
# OpenID of the default super admin — auto-promoted to role=admin on first login.
OWNER_OPEN_ID=
OWNER_NAME=

# -----------------------------------------------------------------------------
# EMAIL (transactional — booking + contact confirmations)
# Transport auto-selected at runtime: Resend > SMTP > console fallback.
#
# Milestone 2 §2.3 — BLOCKED, production activation: the code side (this
# module, the Resend/SMTP/console fallback chain, delivery logging) is
# done — see MILESTONE2_PROGRESS.md §2.3. What's still needed from
# whoever owns the Resend account: (1) a verified sending domain in the
# Resend dashboard (SPF/DKIM DNS records — BOOKING_FROM_EMAIL/SMTP_FROM
# below must be an address at that domain, or Resend will reject sends);
# (2) a production (not test-mode) RESEND_API_KEY; (3) the webhook
# configured per RESEND_WEBHOOK_SECRET's comment below.
# -----------------------------------------------------------------------------
# Option A — Resend (preferred)
RESEND_API_KEY=
# Option B — any SMTP provider (Postmark / SendGrid / SES / Mailgun)
SMTP_URL=
# From address / name used by outbound mail (falls back to SMTP_FROM).
BOOKING_FROM_EMAIL=IO SKY <noreply@iosky.nl>
SMTP_FROM=IO SKY <noreply@iosky.nl>

# Milestone 2 §2.3 — delivery tracking. Signing secret for Resend's
# bounce/complaint/delivery webhook (Resend dashboard → Webhooks → your
# endpoint → "Signing Secret", starts with "whsec_"). Point the webhook at
# POST {PUBLIC_BASE_URL}/api/webhooks/resend, subscribed to at least
# email.delivered/email.bounced/email.complained. Without this set, the
# webhook route rejects every request (fails closed, not silently).
RESEND_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# OWNER ALERTS (Milestone 2 §2.2 — replaces the Manus push-notification
# call). Fires on new leads/bookings/messages/uploads etc.
# (server/_core/notification.ts's notifyOwner(), ~12 call sites). Email
# reuses the same Resend/SMTP transport as the EMAIL section above — no
# separate credential needed for that part. Slack is optional and additive.
# -----------------------------------------------------------------------------
OWNER_NOTIFY_EMAIL=
OWNER_NOTIFY_SLACK_WEBHOOK_URL=

# -----------------------------------------------------------------------------
# SMS (MFA one-time codes) — Twilio
# If unset, the system falls back to a console sender (dev only).
# -----------------------------------------------------------------------------
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# -----------------------------------------------------------------------------
# OBJECT STORAGE (Milestone 2 §2.1 — migrated to Supabase Storage)
# client_documents/client_reports/client_invoices/developer_project_files/
# AI Scan report PDFs are all served through server/storage.ts against
# Supabase Storage now (buckets + RLS in drizzle/0007_storage_buckets.sql,
# applied via SUPABASE_URL/SUPABASE_SECRET_KEY above — no separate env vars
# needed for this part). No S3_* variables are read anywhere in the
# codebase; do not fill them in expecting anything to happen.
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# LEGACY FORGE STORAGE PROXY (branding assets ONLY — logo/mark/favicon)
# server/_core/storageProxy.ts's /manus-storage/* route still calls Forge
# directly for exactly 3 files. BLOCKED from migrating to the Supabase
# `branding` bucket above: the original image bytes exist only in Forge
# storage (no copy anywhere in this repo) and this environment has neither
# Forge nor Supabase credentials to move them. Keep these configured until
# that migration happens — removing them would break the visible site logo.
# See MILESTONE2_PROGRESS.md §2.1 for the exact blocker.
# -----------------------------------------------------------------------------
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# -----------------------------------------------------------------------------
# PRE-LAUNCH STAGING GATE (optional)
# When STAGING_MODE=on, anonymous HTML requests hit a password screen.
# API routes and static assets are never gated. Omit entirely after launch.
# -----------------------------------------------------------------------------
STAGING_MODE=
STAGING_PASSWORD=
# Optional dedicated HMAC secret for the staging cookie (falls back to JWT_SECRET).
STAGING_SECRET=

# -----------------------------------------------------------------------------
# ANALYTICS (optional, frontend beacon)
# -----------------------------------------------------------------------------
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# -----------------------------------------------------------------------------
# BRANDING — not currently read by any code path (page <title> is hardcoded
# in client/index.html; there is no VITE_APP_LOGO consumer anywhere). Left
# here only as a placeholder for future work — do not expect setting these
# to change anything today.
# -----------------------------------------------------------------------------
# VITE_APP_TITLE=IO SKY
# VITE_APP_LOGO=
